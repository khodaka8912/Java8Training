package ch02.ex07;

import java.util.stream.Stream;

/**
 * @author hodaka
 */
public class Streams {

    /**
     * I cannot implement this method.
     * ("T" can replace with "?".)
     */
    public static <T> boolean isFinite(Stream<T> stream) {
        return true;
    }
}
